{
  "type": "{{type}}",
  "name": "{{name}}",
  "package": "{{package}}",
  "attributes": [
    {{#each attributes}}
    {{{json this}}}{{#unless @last}},{{/unless}}
    {{/each}}
  ]
  {{#if relations}}
  ,"relations": [
    {{#each relations}}
    {{{json this}}}{{#unless @last}},{{/unless}}
    {{/each}}
  ]
  {{/if}}
  {{#if crud}}
  ,"crud": {
    "enabled": {{crud.enabled}},
    "path": "{{crud.path}}",
    "disabledMethods": [
      {{#each crud.disabledMethods}}
      "{{this}}"{{#unless @last}},{{/unless}}
      {{/each}}
    ]
  }
  {{/if}}
}
