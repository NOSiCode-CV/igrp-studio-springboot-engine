import java.util.List;
import java.util.Collection;
import java.util.Set;
import java.util.Map;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
{{#if (eq baseConfig.projectStructureStyle "domain")}}
import cv.igrp.framework.core.domain.CommandBus;
import cv.igrp.framework.core.domain.QueryBus;
import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase
resourceConfig.module}}{{else}}shared{{/if}}.application.commands.commands.*;
import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase
resourceConfig.module}}{{else}}shared{{/if}}.application.queries.queries.*;
import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase
resourceConfig.module}}{{else}}shared{{/if}}.application.dto.*;
import {{baseConfig.group}}.{{baseConfig.packageName}}.shared.application.dto.*;

{{importsTypes resourceConfig.actions baseConfig.group baseConfig.packageName resourceConfig.module true}}
{{else}}
{{importsTypes resourceConfig.actions baseConfig.group baseConfig.packageName resourceConfig.module false}}
{{/if}}