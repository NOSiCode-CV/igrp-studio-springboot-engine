{{#if (eq baseConfig.projectStructureStyle "domain")}}
import cv.igrp.core.domain.CommandBus;
import cv.igrp.core.domain.QueryBus;
import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.commands.commands.*;
import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.queries.queries.*;
import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.dto.*;
import java.util.List;
import java.util.Set;
import java.util.Map;
{{else}}
{{importsTypes resourceConfig.actions baseConfig.group baseConfig.packageName resourceConfig.module false}}
{{/if}}
