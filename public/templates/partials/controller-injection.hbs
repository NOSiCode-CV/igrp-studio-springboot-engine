
{{#if (eq baseConfig.projectStructureStyle "domain")}}
private final I{{capitalize resourceConfig.name}}CommandService {{toCamelCase resourceConfig.name}}CmdService;
private final I{{capitalize resourceConfig.name}}QueryService {{toCamelCase resourceConfig.name}}QueryService;
{{else}}
private final I{{resourceConfig.name}}Service {{toCamelCase resourceConfig.name}}Service;
{{/if}}
