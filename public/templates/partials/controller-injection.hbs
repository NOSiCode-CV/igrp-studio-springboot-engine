
{{#if (eq baseConfig.projectStructureStyle "domain")}}
{{#each resourceConfig.actions}}
    {{#if (eq method "GET")}}
private final {{capitalize actionName}}QueryHandler {{toCamelCase actionName}}QueryHandler;
    {{else}}
private final {{capitalize actionName}}CommandHandler {{toCamelCase actionName}}CommandHandler;
    {{/if}}
{{/each}}
{{else}}
private final I{{resourceConfig.name}}Service {{toCamelCase resourceConfig.name}}Service;
{{/if}}
