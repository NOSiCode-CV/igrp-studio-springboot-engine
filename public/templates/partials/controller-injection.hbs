
{{#if (eq baseConfig.projectStructureStyle "domain")}}
private final {{capitalize resourceConfig.name}}CmdService {{toCamelCase resourceConfig.name}}CmdService;
private final {{capitalize resourceConfig.name}}QueryService {{toCamelCase resourceConfig.name}}QueryService;
{{else}}
private final I{{resourceConfig.name}}Service {{toCamelCase resourceConfig.name}}Service;
{{/if}}
