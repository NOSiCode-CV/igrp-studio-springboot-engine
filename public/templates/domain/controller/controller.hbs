package {{baseConfig.group}}.{{baseConfig.artifact}}.controllers.{{resourceConfig.name}};

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.http.HttpStatus;
{{importsTypes resourceConfig.actions baseConfig.group baseConfig.artifact}}
import java.util.List;

@RestController
@RequestMapping(path = "{{resourceConfig.basePath}}")
public class {{capitalize resourceConfig.name}}Controller {

  // Replace {{resourceConfig.name}}ServiceInterface with {{resourceConfig.name}}ServiceInterfaceImpl, which implements all methods defined in SipsServiceInterface. 
  // Note: Ensure that the SipsServiceImpl is injected using the @Autowired annotation, as it is required for this dependency.

  private final {{resourceConfig.name}}ServiceInterface {{toLowerCase resourceConfig.name}}Service;

  public {{resourceConfig.name}}Controller({{resourceConfig.name}}ServiceInterface {{toLowerCase resourceConfig.name}}Service){
    this.{{toLowerCase resourceConfig.name}}Service = {{toLowerCase resourceConfig.name}}Service;
  }

  {{#each resourceConfig.actions}}
  @ResponseStatus(HttpStatus.OK)
  @RequestMapping({{#if path}}value = "{{path}}{{#each pathVariables}}/{{paramsFormat this.name}}{{/each}}", {{/if}}method = RequestMethod.{{method}}{{#if this.accepts}}, consumes = "{{this.accepts}}" {{/if}}{{#if this.contentType}}, produces = "{{this.contentType}}" {{/if}})
  public ResponseEntity<{{{this.response}}}> {{actionName}}({{#if requestBody}}@RequestBody {{this.requestBody}} {{toLowerCase requestBody}}{{/if}}{{#if requestBody}}{{#if requestParams}}, {{/if}}{{/if}}{{#each requestParams}}@RequestParam {{this.type}} {{this.name}}{{#if @last}}{{else}}, {{/if}}{{/each}}{{#if requestParams}}{{#if pathVariables}}, {{/if}}{{/if}}{{#each pathVariables}}@PathVariable {{this.type}} {{this.name}}{{#if @last}}{{else}},{{/if}}{{/each}}) {

    final var result = {{toLowerCase ../resourceConfig.name}}Service.{{actionName}}({{#if requestBody}}{{toLowerCase requestBody}}{{/if}}{{#if requestBody}}{{#if requestParams}}, {{/if}}{{/if}}{{#each requestParams}}{{this.name}}{{#if @last}}{{else}}, {{/if}}{{/each}}{{#if requestParams}}{{#if pathVariables}}, {{/if}}{{/if}}{{#each pathVariables}}{{this.name}}{{#if @last}}{{else}}, {{/if}}{{/each}});

    return ResponseEntity.ok(result);
  }
  
  {{/each}}
}