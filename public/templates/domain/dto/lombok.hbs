package {{baseConfig.group}}.{{baseConfig.artifact}}.dto;

import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;

{{{resolve-imports resourceConfig}}}

@Data
@NoArgsConstructor
@AllArgsConstructor
{{#each resourceConfig.annotations}}
@{{this.annotation}}
{{/each}}
public class {{resourceConfig.name}}{{#if resourceConfig.generics}}<{{#each resourceConfig.generics}}{{this}}{{#if @last}}{{else}}, {{/if}}{{/each}}>{{/if}}{

    {{#each resourceConfig.attributes}}
    {{#each this.annotations}}
    @{{this.annotation}}
    {{/each}}
    private {{{resolve-type this.type}}} {{this.name}};

    {{/each}}
}