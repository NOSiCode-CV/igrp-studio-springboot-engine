package {{baseConfig.group}}.{{baseConfig.artifact}}.dto;

{{resolve-imports resourceConfig}}

{{#each resourceConfig.annotations}}
@{{this.annotation}}
{{/each}}
public record {{resourceConfig.name}}(
{{#each resourceConfig.attributes}}
    {{#each this.annotations}}
    @{{this.annotation}}
    {{/each}}
    {{{resolve-type this.type}}} {{this.name}}{{#if @last}}{{else}}, {{/if}}
{{/each}}
){};