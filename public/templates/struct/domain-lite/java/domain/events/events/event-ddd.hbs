package {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.domain.events.events;

import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
{{{resolve-imports resourceConfig}}}
@Data
@NoArgsConstructor
@AllArgsConstructor
{{#each resourceConfig.annotations}}
  @{{this.annotation}}
{{/each}}
public class {{resourceConfig.name}}Event {

{{#each resourceConfig.attributes}}
  {{#each this.annotations}}
    @{{this.annotation}}
  {{/each}}
  private {{{resolve-type this}}} {{this.name}};

{{/each}}
}