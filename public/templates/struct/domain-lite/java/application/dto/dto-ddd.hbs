{{> package-java }}

import cv.igrp.framework.stereotype.IgrpDTO;
import jakarta.validation.*;
import jakarta.validation.constraints.*;
{{#if resourceConfig.attributes}}
{{> lombok-java-imports}}
{{/if}}
{{#if resourceConfig.enableCustonValidation}}
import {{baseConfig.group}}.{{baseConfig.artifact}}.{{resourceConfig.module}}.application.dto.validator.{{resolve-interface-custon-validator-name resourceConfig.name}};
{{/if}}
{{{resolve-imports resourceConfig baseConfig}}}
{{#if resourceConfig.attributes}}
{{> lombok-java-annotations}}
{{/if}}

{{#each resourceConfig.annotations}}
@{{this.annotation}}
{{/each}}
{{#if resourceConfig.enableCustonValidation}}
@{{resolve-interface-custon-validator-name resourceConfig.name}}
{{/if}}
@IgrpDTO
public class {{resourceConfig.name}}DTO {{#if resourceConfig.extends}}extends {{resourceConfig.extends.name}}DTO{{/if}}{

{{#each resourceConfig.attributes}}
  {{#each this.annotations}}
  @{{this.annotation}}
  {{/each}}
  {{{resolve-annotations this}}}
  {{{is-nested-object this}}}
  private {{{resolve-type this}}} {{this.name}} {{{getCollectionTypeInitializer this}}};
{{/each}}

}