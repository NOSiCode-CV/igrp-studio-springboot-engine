{{> package-java }}

import org.springframework.stereotype.Service;

import {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.dto.*;
import {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.queries.queries.{{resourceConfig.name}}Query;

import java.util.List;

@Service
public class {{capitalize resourceConfig.name}}QueryHandler {

   public {{capitalize resourceConfig.name}}QueryHandler() {

   }

   public {{cleanStr resourceConfig.response}} handle({{capitalize resourceConfig.name}}Query query) {
      // TODO: Implement the query handling logic here
      return null;
   }

}
