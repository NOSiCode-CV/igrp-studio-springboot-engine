package {{baseConfig.group}}.{{baseConfig.artifact}}.application.queries.handlers;

import org.springframework.stereotype.Service;

@Service
public class {{resourceConfig.name}}QueryHandler {

   public {{resourceConfig.name}}QueryHandler() {

   }

   public {{resourceConfig.response}} handle({{resourceConfig.name}}Query query) {
      // TODO: Implement the query handling logic here
   }

}
