{{> package-java }}

import cv.igrp.framework.core.domain.CommandHandler;
import cv.igrp.framework.stereotype.IgrpCommandHandler;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;

import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase
resourceConfig.module}}{{else}}shared{{/if}}.application.dto.*;

import {{baseConfig.group}}.{{baseConfig.packageName}}.shared.application.dto.*;


import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase
resourceConfig.module}}{{else}}shared{{/if}}.application.commands.commands.{{resourceConfig.name}}Command;

import java.util.List;
import java.util.Collection;
import java.util.Map;
import org.springframework.data.domain.Page;

@Service
public class {{capitalize resourceConfig.name}}CommandHandler implements CommandHandler<{{capitalize
   resourceConfig.name}}Command, ResponseEntity<{{{resourceConfig.response}}}>> {

   public {{capitalize resourceConfig.name}}CommandHandler() {

   }

   @IgrpCommandHandler
   public ResponseEntity<{{{resourceConfig.response}}}> handle({{capitalize resourceConfig.name}}Command
      command) {
      // TODO: Implement the command handling logic here
      return null;
      }

      }