{{> package-java }}

import org.springframework.stereotype.Service;

import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.dto.*;

import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.commands.commands.{{resourceConfig.name}}Command;

import java.util.List;

@Service
public class {{capitalize resourceConfig.name}}CommandHandler {

   public {{capitalize resourceConfig.name}}CommandHandler() {

   }

   public {{resourceConfig.response}} handle({{capitalize resourceConfig.name}}Command command) {
      // TODO: Implement the command handling logic here
      return null;
   }

}
