{{> package-java }}

/*
 * Implementation class for {{resourceConfig.name}}ServiceInterface.
 * 
 * This class is generated once and serves as a placeholder for the business logic 
 * of the {{resourceConfig.name}} service. Any custom implementations or modifications added here 
 * will not be overridden by future code generations.
 *
 */

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
{{importsTypes resourceConfig.actions baseConfig.group baseConfig.packageName resourceConfig.module}}
import {{baseConfig.group}}.{{baseConfig.packageName}}.controllers.{{toLowerCase resourceConfig.name}}.{{resourceConfig.name}}ServiceInterface;

@Service
public class {{resourceConfig.name}}ServiceImpl implements {{resourceConfig.name}}ServiceInterface {

  private static final Logger logger = LoggerFactory.getLogger({{resourceConfig.name}}ServiceImpl.class);

  {{#each resourceConfig.actions}}
  @Override
  public {{{response}}} {{actionName}}({{#if requestBody}}{{{requestBody}}} {{toLowerCase requestBody}}{{/if}}{{#if (and requestBody requestParams)}}, {{/if}}{{#if (and pathVariables requestBody)}}, {{/if}}{{#each requestParams}}{{resolve-type this}} {{this.name}}{{#if @last}}{{else}}, {{/if}}{{/each}}{{#if (and requestParams pathVariables)}}, {{/if}}{{#each pathVariables}}{{resolve-type this}} {{this.name}}{{#if @last}}{{else}}, {{/if}}{{/each}}){
    // TODO: implement this method
    return null;
  }

  {{/each}}
}