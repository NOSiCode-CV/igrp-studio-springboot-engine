package {{baseConfig.group}}.{{baseConfig.artifact}}.domain.exceptions;

public class {{resourceConfig.name}}Exception extends {{resourceConfig.baseException}} {

   public {{resourceConfig.name}}Exception(String message) {
      super(message != null ? message : "An exception of type {{resourceConfig.name}} was thrown. Please try again later");
   }

   public {{resourceConfig.name}}Exception(String message, Throwable cause) {
      super(message != null ? message : "An exception of type {{resourceConfig.name}} was thrown. Please try again later", cause);
   }

   public {{resourceConfig.name}}Exception(Throwable cause) {
      super("An exception of type {{resourceConfig.name}} was thrown. Please try again later", cause);
   }
}