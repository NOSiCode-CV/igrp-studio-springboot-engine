package {{baseConfig.group}}.{{baseConfig.artifact}}.domain.event;

import java.util.List;

public interface EventBus {

  void publish(Event event);

  default void publishAll(List<Event> events) {
    for (Event event : events) {
       publish(event);
    }
  }
}