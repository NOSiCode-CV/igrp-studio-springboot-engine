package {{baseConfig.group}}.{{baseConfig.packageName}}.domain.event.{{toLowerCase resourceConfig.aggregate}};

import {{baseConfig.group}}.{{baseConfig.packageName}}.domain.event.EventListener;
import org.springframework.stereotype.Component;

@Component
public class {{resourceConfig.name}}EventListener {

   @EventListener
   public void handle({{resourceConfig.name}}Event event) {
      // TODO: Implement the command listening logic here
      // Example content for demonstration purposes:
      // String content = "The name was sent successfully.";
      // System.out.println(content);
   }

}
