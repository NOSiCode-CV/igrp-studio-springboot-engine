package {{baseConfig.group}}.{{baseConfig.artifact}}.domain.aggregate.{{toLowerCase resourceConfig.name}};

/* This code should not be edited since it's generated by IGRP Studio. Any changes here will be erased in the next generation */

{{#each resourceConfig.actions}}
  {{#ifNot (eq this.method "GET")}}
import {{../baseConfig.group}}.{{../baseConfig.artifact}}.application.command.{{this.actionName}}Command;
  {{/ifNot}}
{{/each}}
import {{baseConfig.group}}.{{baseConfig.artifact}}.domain.aggregate.Aggregate;
import {{baseConfig.group}}.{{baseConfig.artifact}}.domain.aggregate.AggregateRoot;

@Aggregate
public abstract class {{resourceConfig.name}}AggregateRoot extends AggregateRoot<{{resourceConfig.name}}AggregateRoot, {{keyType resourceConfig}}> {

{{#each resourceConfig.attributes}}
  {{#if this.primaryKey}}
   private {{keyType ../resourceConfig}} {{this.name}};
  {{else}}
   private {{{resolve-type this}}} {{this.name}};
   {{/if}}
{{/each}}

   {{#each resourceConfig.actions}}
     {{#ifNot (eq this.method "GET")}}
   public abstract void handle({{this.actionName}}Command cmd);
     {{/ifNot}}
   {{/each}}
}