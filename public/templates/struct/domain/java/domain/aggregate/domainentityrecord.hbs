package {{baseConfig.group}}.{{baseConfig.artifact}}.domain.aggregate.{{resourceConfig.aggregate.toLowerCase}}Aggregate;

import {{baseConfig.group}}.{{baseConfig.artifact}}.domain.DomainEntity;

{{resolve-imports resourceConfig}}

{{#each resourceConfig.annotations}}
@{{this.annotation}}
{{/each}}
public record {{resourceConfig.name}}DomainEntity (
{{#each resourceConfig.attributes}}
    {{#each this.annotations}}
    @{{this.annotation}}
    {{/each}}
    {{{resolve-type this}}} {{this.name}}{{#if @last}}{{else}}, {{/if}}
{{/each}}
) extends DomainEntity<{{keyType resourceConfig}}> {}