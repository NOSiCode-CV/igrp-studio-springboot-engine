package {{baseConfig.group}}.{{baseConfig.artifact}}.domain.aggregate.{{toLowerCase resourceConfig.name}}Aggregate;

{{#each resourceConfig.actions}}
  {{#if (eq this.method "POST")}}
import {{baseConfig.group}}.{{baseConfig.artifact}}.application.command.{{this.actionName}}Command;
  {{else if (eq this.method "PUT")}}
import {{baseConfig.group}}.{{baseConfig.artifact}}.application.command.{{this.actionName}}Command;
  {{/if}}
{{/each}}
import {{baseConfig.group}}.{{baseConfig.artifact}}.domain.aggregate.Aggregate
import {{baseConfig.group}}.{{baseConfig.artifact}}.domain.aggregate.AggregateRoot

@Aggregate
public class {{resourceConfig.name}}AggregateRoot extends AggregateRoot<{{resourceConfig.name}}AggregateRoot, {{keyType resourceConfig}}> {

   private final {{keyType resourceConfig}} id;

   // TODO: define the aggregate attributes

   public {{resourceConfig.name}}({{keyType resourceConfig}} id{{#each resourceConfig.attributes}}, {{{resolve-type this}}} {{this.name}}{{/each}}) {
      this.id = id;
      // TODO: define the aggregate attributes
   }

   {{#each resourceConfig.actions}}
     {{#if (eq this.method "POST")}}
       public void handle({{this.actionName}}Command cmd) {
       // TODO: Implement the {{this.actionName}} command handling logic here
       }
     {{else if (eq this.method "PUT")}}
       public void handle({{this.actionName}}Command cmd) {
       // TODO: Implement the {{this.actionName}} command handling logic here
       }
     {{/if}}
   {{/each}}
}