{{> package-java }}

{{#each resourceConfig.actions}}
  {{#if (eq this.method "GET")}}
import {{../baseConfig.group}}.{{../baseConfig.packageName}}.{{toLowerCase ../resourceConfig.module}}.application.queries.queries.{{this.actionName}}Query;
  {{/if}}
{{/each}}
import java.util.List;
import org.springframework.stereotype.Service;
import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.dto.*;

@Service
public interface {{capitalize resourceConfig.name}}QueryService {

{{#each resourceConfig.actions}}
  {{#if (eq this.method "GET")}}

     {{cleanStr this.response}} {{toCamelCase this.actionName}}({{capitalize this.actionName}}Query query);

  {{/if}}
{{/each}}
}