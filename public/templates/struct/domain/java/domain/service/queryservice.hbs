package {{baseConfig.group}}.{{baseConfig.artifact}}.domain.service.{{resourceConfig.name.toLowerCase}};

import {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.dto.{{resourceConfig.name}}DTO;
import {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.dto.{{resourceConfig.name}}AggregateDTO;
import java.util.List;

public interface {{resourceConfig.name}}QueryService {

   List<{{resourceConfig.name}}DTO> query{{resourceConfig.name}}s();

   {{resourceConfig.name}}DTO query{{resourceConfig.name}}({{keyType resourceConfig}} id);

   {{resourceConfig.name}}AggregateDTO query{{resourceConfig.name}}Aggregate({{keyType resourceConfig}} id);

}