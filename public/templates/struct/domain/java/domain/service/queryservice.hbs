package {{baseConfig.group}}.{{baseConfig.artifact}}.{{resourceConfig.module}}.domain.service;

{{#each resourceConfig.actions}}
  {{#if (eq this.method "GET")}}
import {{../baseConfig.group}}.{{../baseConfig.artifact}}.{{toLowerCase ../resourceConfig.module}}.application.queries.queries.{{this.actionName}}Query;
  {{/if}}
{{/each}}
import java.util.List;
import org.springframework.stereotype.Service;

@Service
public interface {{resourceConfig.name}}QueryService {

{{#each resourceConfig.actions}}
  {{#if (eq this.method "GET")}}

     {{cleanStr this.response}} {{toCamelCase this.actionName}}({{this.actionName}}Query query);

  {{/if}}
{{/each}}
}