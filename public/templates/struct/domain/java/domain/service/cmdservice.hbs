package {{baseConfig.group}}.{{baseConfig.artifact}}.domain.service;

{{#each resourceConfig.actions}}
  {{#ifNot (eq this.method "GET")}}
import {{../baseConfig.group}}.{{../baseConfig.artifact}}.{{toLowerCase ../resourceConfig.module}}.application.commands.commands.{{this.actionName}}Command;
  {{/ifNot}}
{{/each}}

import org.springframework.stereotype.Service;

@Service
public interface {{resourceConfig.name}}CmdService {

{{#each resourceConfig.actions}}
  {{#ifNot (eq this.method "GET")}}
    {{cleanStr this.response}} {{toCamelCase this.actionName}}({{this.actionName}}Command cmd);
  {{/ifNot}}
{{/each}}

}