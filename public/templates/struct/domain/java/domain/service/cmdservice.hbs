package {{baseConfig.group}}.{{baseConfig.artifact}}.domain.service.{{toLowerCase resourceConfig.name}};

{{#each resourceConfig.actions}}
  {{#ifNot (eq this.method "GET")}}
import {{../baseConfig.group}}.{{../baseConfig.artifact}}.application.command.{{toLowerCase ../resourceConfig.name}}.{{this.actionName}}Command;
  {{/ifNot}}
{{/each}}

import org.springframework.stereotype.Service;

@Service
public interface {{resourceConfig.name}}CmdService {

   {{#each resourceConfig.actions}}
     {{#ifNot (eq this.method "GET")}}
   void {{toCamelCase this.actionName}}({{this.actionName}}Command cmd);
     {{/ifNot}}
   {{/each}}

}