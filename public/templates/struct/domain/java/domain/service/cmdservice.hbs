package {{baseConfig.group}}.{{baseConfig.artifact}}.domain.service.{{resourceConfig.name.toLowerCase}};

import {{#each resourceConfig.actions}}{{baseConfig.group}}.{{baseConfig.artifact}}.application.command.{{this.actionName}}Command;{{/each}}
import org.springframework.stereotype.Service;

public interface {{resourceConfig.name}}CmdService {

   {{#each resourceConfig.actions}}
     void {{this.actionName}}({{this.actionName}}Command cmd);
   {{/each}}

}