{{> package-java }}

{{#each resourceConfig.actions}}
  {{#ifNot (eq this.method "GET")}}
import {{../baseConfig.group}}.{{../baseConfig.packageName}}.{{#if ../resourceConfig.module}}{{toLowerCase ../resourceConfig.module}}{{else}}shared{{/if}}.application.commands.commands.{{this.actionName}}Command;
  {{/ifNot}}
{{/each}}

import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.dto.*;
import org.springframework.stereotype.Service;
import java.util.List;

@Service
public interface {{capitalize resourceConfig.name}}CmdService {

{{#each resourceConfig.actions}}
  {{#ifNot (eq this.method "GET")}}
    {{cleanStr this.response}} {{toCamelCase this.actionName}}({{capitalize this.actionName}}Command cmd);
  {{/ifNot}}
{{/each}}

}