package {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.domain.service;

import {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.commands.commands.*;
import {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.commands.handlers.*;
import {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.dto.*;
import org.springframework.stereotype.Service;

@Service
public class {{resourceConfig.name}}CmdServiceImpl implements {{resourceConfig.name}}CmdService {

{{#each resourceConfig.actions}}
  {{#ifNot (eq method "GET")}}
    private final {{actionName}}CommandHandler {{toCamelCase actionName}}CommandHandler;
  {{/ifNot}}
{{/each}}

    public {{resourceConfig.name}}CmdServiceImpl(
        {{#filterCommandActions resourceConfig.actions}}{{actionName}}CommandHandler {{toCamelCase actionName}}CommandHandler{{/filterCommandActions}}
    ) {
    {{#each resourceConfig.actions}}
      {{#ifNot (eq method "GET")}}
        this.{{toCamelCase actionName}}CommandHandler = {{toCamelCase actionName}}CommandHandler;
      {{/ifNot}}
    {{/each}}
    }

{{#each resourceConfig.actions}}
  {{#ifNot (eq this.method "GET")}}
    @Override
    public {{cleanStr this.response}} {{toCamelCase this.actionName}}({{this.actionName}}Command cmd) {
    return {{toCamelCase this.actionName}}CommandHandler.handle(cmd);
    }

  {{/ifNot}}
{{/each}}
}