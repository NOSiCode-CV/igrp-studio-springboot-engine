package {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.domain.service;

import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.commands.commands.*;
import {{baseConfig.group}}.{{baseConfig.packageName}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.commands.handlers.*;
import {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.dto.*;
import org.springframework.stereotype.Service;

@Service
public class {{capitalize resourceConfig.name}}CmdServiceImpl implements {{capitalize resourceConfig.name}}CmdService {

{{#each resourceConfig.actions}}
  {{#ifNot (eq method "GET")}}
    private final {{capitalize actionName}}CommandHandler {{toCamelCase actionName}}CommandHandler;
  {{/ifNot}}
{{/each}}

    public {{capitalize resourceConfig.name}}CmdServiceImpl(
        {{#filterCommandActions resourceConfig.actions}}{{capitalize actionName}}CommandHandler {{toCamelCase actionName}}CommandHandler{{/filterCommandActions}}
    ) {
    {{#each resourceConfig.actions}}
      {{#ifNot (eq method "GET")}}
        this.{{toCamelCase actionName}}CommandHandler = {{toCamelCase actionName}}CommandHandler;
      {{/ifNot}}
    {{/each}}
    }

{{#each resourceConfig.actions}}
  {{#ifNot (eq this.method "GET")}}
    @Override
    public {{cleanStr this.response}} {{toCamelCase this.actionName}}({{capitalize this.actionName}}Command cmd) {
        return {{toCamelCase this.actionName}}CommandHandler.handle(cmd);
    }

  {{/ifNot}}
{{/each}}
}