package {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.domain.service;

import {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.queries.queries.*;
import {{baseConfig.group}}.{{baseConfig.artifact}}.{{#if resourceConfig.module}}{{toLowerCase resourceConfig.module}}{{else}}shared{{/if}}.application.queries.handlers.*;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class {{resourceConfig.name}}QueryServiceImpl implements {{resourceConfig.name}}QueryService {

{{#each resourceConfig.actions}}
  {{#if (eq method "GET")}}
    private final {{actionName}}QueryHandler {{toCamelCase actionName}}QueryHandler;
  {{/if}}
{{/each}}

    public {{resourceConfig.name}}QueryServiceImpl(
    {{#each resourceConfig.actions}}
      {{#if (eq method "GET")}}
        {{actionName}}QueryHandler {{toCamelCase actionName}}QueryHandler{{#unless @last}},{{/unless}}
      {{/if}}
    {{/each}}
    ) {
    {{#each resourceConfig.actions}}
      {{#if (eq method "GET")}}
        this.{{toCamelCase actionName}}QueryHandler = {{toCamelCase actionName}}QueryHandler;
      {{/if}}
    {{/each}}
    }

{{#each resourceConfig.actions}}
  {{#if (eq this.method "GET")}}
    @Override
    public {{cleanStr this.response}} {{toCamelCase this.actionName}}({{this.actionName}}Query query) {
    return {{toCamelCase this.actionName}}QueryHandler.handle(query);
    }

  {{/if}}
{{/each}}
}