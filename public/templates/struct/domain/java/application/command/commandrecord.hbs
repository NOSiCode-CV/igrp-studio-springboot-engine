package {{baseConfig.group}}.{{baseConfig.artifact}}.application.command.{{resourceConfig.aggregate.toLowerCase}};

import {{baseConfig.group}}.{{baseConfig.artifact}}.application.command.Command;

{{resolve-imports resourceConfig}}

{{#each resourceConfig.annotations}}
@{{this.annotation}}
{{/each}}
public record {{capitalize resourceConfig.name}}Command (
{{#each resourceConfig.attributes}}
    {{#each this.annotations}}
    @{{this.annotation}}
    {{/each}}
    {{{resolve-type this}}} {{this.name}}{{#if @last}}{{else}}, {{/if}}
{{/each}}
) implements Command {}