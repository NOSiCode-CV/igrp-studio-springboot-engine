package {{baseConfig.group}}.{{baseConfig.artifact}}.application.command.{{toLowerCase resourceConfig.aggregate}};

import {{baseConfig.group}}.{{baseConfig.artifact}}.application.command.CommandListener;

import {{baseConfig.group}}.{{baseConfig.artifact}}.domain.event.EventBus;
import org.springframework.stereotype.Component;

@Component
public class {{resourceConfig.name}}CommandListener {

   private final EventBus eventBus;

   public {{resourceConfig.name}}CommandListener(EventBus eventBus) {
      this.eventBus = eventBus;
   }

   @CommandListener
   public void handle({{resourceConfig.name}}Command command) {
      // TODO: Implement the command listening logic here
      // Example content for demonstration purposes:
      // String content = "The name sent is " + command.getName() + ".";
      // System.out.println(content);

      // Publish event to EventBus:
      // eventBus.publish(new NamePrintedEvent(
      //     command.getName(),
      //     command.getNif()
      // ));
   }
}
