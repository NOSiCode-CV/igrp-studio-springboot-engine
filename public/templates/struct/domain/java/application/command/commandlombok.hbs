package {{baseConfig.group}}.{{baseConfig.artifact}}.application.command.{{toLowerCase resourceConfig.aggregate}};

import {{baseConfig.group}}.{{baseConfig.artifact}}.application.command.Command;

import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;

{{{resolve-imports resourceConfig}}}

@Data
@NoArgsConstructor
@AllArgsConstructor
{{#each resourceConfig.annotations}}
@{{this.annotation}}
{{/each}}
public class {{resourceConfig.name}}Command implements Command {

    {{#each resourceConfig.attributes}}
    {{#each this.annotations}}
    @{{this.annotation}}
    {{/each}}
    private {{{resolve-type this}}} {{this.name}};

    {{/each}}
}