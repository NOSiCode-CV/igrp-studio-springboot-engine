package {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.assembler.{{toLowerCase resourceConfig.aggregate}};

import {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.dto.{{resourceConfig.name}}DTO;
import {{baseConfig.group}}.{{baseConfig.artifact}}.domain.aggregate.{{toLowerCase resourceConfig.name}}Aggregate.{{resourceConfig.name}}AggregateRoot;
import org.springframework.stereotype.Component;

@Component
public class {{resourceConfig.name}}AggregateAssembler implements Assembler<{{resourceConfig.name}}AggregateRoot, {{resourceConfig.name}}AggregateDTO> {

   @Override
   public {{resourceConfig.name}}AggregateDTO apply({{resourceConfig.name}}AggregateRoot t) {
      // Replace the constructor with the actual implementation as needed.
      // Example:
      return new {{resourceConfig.name}}AggregateDTO(
         // Map the fields here
      );
   }

}
