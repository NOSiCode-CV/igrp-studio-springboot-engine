package {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.assembler;

import {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.dto.{{entityName}}DTO;
import {{baseConfig.group}}.{{baseConfig.artifact}}.domain.aggregate.{{entityNameLower}}Aggregate.{{entityName}}AggregateRoot;
import org.springframework.stereotype.Component;

@Component
public class {{entityName}}AggregateAssembler implements Assembler<{{entityName}}AggregateRoot, {{entityName}}AggregateDTO> {

   @Override
   public {{entityName}}AggregateDTO apply({{entityName}}AggregateRoot t) {
      // Replace the constructor with the actual implementation as needed.
      // Example:
      return new {{entityName}}AggregateDTO(
         // Map the fields here
      );
   }

}
