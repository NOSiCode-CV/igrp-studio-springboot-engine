package {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.assembler.{{resourceConfig.aggregate.toLowerCase}};

import {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.dto.{{resourceConfig.name}}DTO;
import {{baseConfig.group}}.{{baseConfig.artifact}}.infrastructure.db.dataobject.{{resourceConfig.aggregate.toLowerCase}}.{{resourceConfig.name}}DO;
import org.springframework.stereotype.Component;

@Component
public class {{resourceConfig.name}}Assembler implements Assembler<{{resourceConfig.name}}DO, {{resourceConfig.name}}DTO> {

   @Override
   public {{resourceConfig.name}}DTO apply({{resourceConfig.name}}DO t) {
      // Replace the constructor with the actual implementation as needed.
      // Example:
      return new {{resourceConfig.name}}DTO(
         // Map the fields here
      );
   }

}
