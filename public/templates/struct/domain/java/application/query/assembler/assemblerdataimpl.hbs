package {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.assembler;

import {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.dto.{{entityName}}DTO;
import {{baseConfig.group}}.{{baseConfig.artifact}}.infrastructure.db.dataobject.{{entityNameLower}}.{{entityName}}DO;
import org.springframework.stereotype.Component;

@Component
public class {{entityName}}Assembler implements Assembler<{{entityName}}DO, {{entityName}}DTO> {

   @Override
   public {{entityName}}DTO apply({{entityName}}DO t) {
      // Replace the constructor with the actual implementation as needed.
      // Example:
      return new {{entityName}}DTO(
         // Map the fields here
      );
   }

}
