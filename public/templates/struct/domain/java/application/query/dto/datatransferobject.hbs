package {{baseConfig.group}}.{{baseConfig.packageName}}.application.query.dto;

public abstract class DataTransferObject<T> {

   private final T id;

   public DataTransferObject(T id) {
     this.id = id;
   }

   public T getId() {
      return id;
   }
}