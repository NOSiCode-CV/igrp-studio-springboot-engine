package {{baseConfig.group}}.{{baseConfig.packageName}}.application.query.dto.{{toLowerCase resourceConfig.aggregate}};

{{> lombok-java-imports}}

{{{resolve-imports resourceConfig baseConfig basePath}}}

{{> lombok-java-annotations}}
{{#each resourceConfig.annotations}}
  @{{this.annotation}}
{{/each}}
public class {{resourceConfig.name}}DTO {

  {{#each resourceConfig.attributes}}
    {{#each this.annotations}}
      @{{this.annotation}}
    {{/each}}
    private {{{resolve-type this}}} {{this.name}};
  {{/each}}

}