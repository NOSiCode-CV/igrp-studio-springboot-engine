package {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.dto.{{toLowerCase resourceConfig.aggregate}};

import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
import {{baseConfig.group}}.{{baseConfig.artifact}}.application.query.dto.DataTransferObject;

{{{resolve-imports resourceConfig}}}

@Data
@NoArgsConstructor
@AllArgsConstructor
{{#each resourceConfig.annotations}}
  @{{this.annotation}}
{{/each}}
public class {{resourceConfig.name}}DTO extends DataTransferObject<{{keyType resourceConfig}}> {

  {{#each resourceConfig.attributes}}
    {{#each this.annotations}}
      @{{this.annotation}}
    {{/each}}
    private {{{resolve-type this}}} {{this.name}};
  {{/each}}

}