package {{baseConfig.group}}.{{baseConfig.packageName}}.application.query.{{toLowerCase resourceConfig.aggregate}};

import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
{{{resolve-imports resourceConfig}}}

@Data
@NoArgsConstructor
@AllArgsConstructor
{{#each resourceConfig.annotations}}
@{{this.annotation}}
{{/each}}
public class {{resourceConfig.name}}Query {

    {{#each resourceConfig.attributes}}
    {{#each this.annotations}}
    @{{this.annotation}}
    {{/each}}
    private {{{resolve-type this}}} {{this.name}};

    {{/each}}
}