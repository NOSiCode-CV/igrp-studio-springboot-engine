package {{baseConfig.group}}.{{baseConfig.artifact}}.infrastructure.db.dataobject.{{toLowerCase resourceConfig.aggregate}};

import {{baseConfig.group}}.{{baseConfig.artifact}}.infrastructure.db.dataobject.DataObject;

{{resolve-imports resourceConfig}}

{{#each resourceConfig.annotations}}
@{{this.annotation}}
{{/each}}
public record {{capitalize resourceConfig.name}}DataObject (
{{#each resourceConfig.attributes}}
    {{#each this.annotations}}
    @{{this.annotation}}
    {{/each}}
    {{{resolve-type this}}} {{this.name}}{{#if @last}}{{else}}, {{/if}}
{{/each}}
) extends DataObject<{{keyType resourceConfig}}> {}