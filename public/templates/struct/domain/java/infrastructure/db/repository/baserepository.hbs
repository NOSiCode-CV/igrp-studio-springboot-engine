{{> package-java }}

import {{baseConfig.group}}.{{baseConfig.artifact}}.infrastructure.db.entity.EntityBase;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.data.repository.NoRepositoryBean;

@NoRepositoryBean
public interface BaseRepository <T extends EntityBase<T, ID>, ID> extends JpaRepository<T, ID>, JpaSpecificationExecutor<T> {
}