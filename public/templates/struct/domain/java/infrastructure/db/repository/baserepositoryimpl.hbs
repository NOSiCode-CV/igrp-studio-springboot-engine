package {{baseConfig.group}}.{{baseConfig.artifact}}.infrastructure.db.repository.{{resourceConfig.name.toLowerCase}};

import {{baseConfig.group}}.{{baseConfig.artifact}}.infrastructure.db.entity.{{resourceConfig.name.toLowerCase}}.{{resourceConfig.name}}Entity;
import {{baseConfig.group}}.{{baseConfig.artifact}}.infrastructure.db.repository.BaseRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface {{resourceConfig.name}}Repository extends BaseRepository<{{resourceConfig.name}}Entity, {{keyType resourceConfig}}> {
}